doctype html
html
  head
    title Prata med andra frihetsälskare på vår Slack!
    style.
      body {
        padding: 50px;
        font: 14px "Lucida Grande", Helvetica, Arial, sans-serif;
      }
      input {
        font-size: 24px;
        width: 400px;
      }
  body
    h1 Prata med andra frihetsälskare på vår Slack!
    form(method="POST", action="/invite")#join-form
      input(type="text", name="email", placeholder="Fyll i din e-postadress")#slack-email
      input(type="submit", value="Skicka mig en inbjudan")
    script.
      var form = document.getElementById('join-form');
      var email = document.getElementById('slack-email')
      form.addEventListener('submit', function(evt) {
        if (!email.value) {
          evt.preventDefault();
        }
      });
